"""Test doubles for the Kalshi sync core, shared by the unit and integration
tiers (slice 262). Importable from any tier because ``test/`` is on
``sys.path`` via the root ``conftest.py`` (the ``_prod_url_guard`` precedent).
"""
